<!-- miniprogram/pages/cert/cert.wxml -->
<view class="container">
  <!-- 顶部状态栏 -->
  <view class="step-header">
    <view class="step-item active">1. 填写信息</view>
    <view class="step-line"></view>
    <view class="step-item">2. 身份验证</view>
  </view>

  <!-- 模拟学生证卡片 -->
  <view class="student-card">
    <view class="card-header">
      <view class="logo-area">
        <text class="logo-text">BUCM | 1956</text>
      </view>
      <view class="card-title">校园通行证</view>
    </view>
    
    <view class="card-body">
      <view class="input-group">
        <text class="label">姓名</text>
        <input class="neo-input" placeholder="请输入真实姓名" placeholder-class="ph-style" />
      </view>
      
      <view class="input-group">
        <text class="label">学号</text>
        <input class="neo-input" type="number" placeholder="请输入 11 位学号" placeholder-class="ph-style" />
      </view>

      <view class="input-group">
        <text class="label">院系</text>
        <picker mode="selector" range="{{departments}}" bindchange="onDeptChange">
          <view class="neo-input {{selectedDept ? '' : 'ph-style'}}">
            {{selectedDept || '请选择所属院系'}}
          </view>
        </picker>
      </view>
    </view>

    <view class="card-footer">
      <text class="footer-tip">* 信息仅用于身份核验，平台严格保密</text>
    </view>
  </view>

  <!-- 提示区域 -->
  <view class="notice-box">
    <view class="notice-title">💡 认证须知</view>
    <view class="notice-content">
      <text>1. 认证后可获得“北中医在校生”专属标识。
</text>
      <text>2. 认证用户可参与集市交易与校内专属话题讨论。
</text>
      <text>3. 请确保学号与姓名一致，否则将审核失败。</text>
    </view>
  </view>

  <!-- 提交按钮 -->
  <view class="btn-area">
    <button class="submit-btn" bindtap="onSubmit">下一步：验证身份</button>
  </view>
</view>